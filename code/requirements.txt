# Python dependencies for the case study example scripts.
# Install with:  pip install -r code/requirements.txt
#
# We use:
#   - pandas for tabular data wrangling (the pandas analog of dplyr)
#   - igraph (python-igraph) as the primary network library; names parallel R's igraph
#   - networkx where it makes drawing or specific algorithms easier
#   - numpy for arrays and Monte Carlo
#   - matplotlib + seaborn for plotting
#   - pyarrow so pandas can read .parquet files
#   - scikit-learn + xgboost for the prediction cases
#   - geopandas + shapely for the spatial sampling case

pandas>=2.0
numpy>=1.24
igraph>=0.11
networkx>=3.0
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
xgboost>=2.0
geopandas>=0.14
shapely>=2.0
